{
  "name": "justbuild-hackathon-database",
  "version": "1.0.0",
  "description": "Shared database for Justbuild Hackathon projects",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma db push",
    "db:studio": "prisma studio",
    "db:reset": "prisma db push --force-reset"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "prisma": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0"
  }
}